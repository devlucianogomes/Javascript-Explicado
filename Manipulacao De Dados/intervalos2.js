// Usando o setTimeOut, onde ele aguarda os segundos, executa a função e finaliza posteriormente

setTimeout(function () {
  console.log("Fui executado após 2s !");
}, 2000);

// Para parar o timeout, antes que a funcao seja finalizada, podemos usar um clearTimeoOut
