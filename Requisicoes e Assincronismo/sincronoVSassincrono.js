// Entendendo a diferenca do código síncrono e código assíncrono

// SÍNCRONO -> executa linha a linha, e só avança para a próxima linha quando a linha anterior é finaliada

// Código Síncrono, sendo executado linha a linha
let nome = "luciano";
let sobrenome = "gomes";
let completo = nome + " " + sobrenome;

// ASSÍNCRONO -> O código flue de forma normal, porém sem esperar que todas as linhas sejam satisfeitas, ou seja, se tal código irá demorar um pouco para ser executado, ele passará por ela da mesma forma. Quando a linha assincrona for recebida, ele armazenará a resposta em algo, e em seguida fazer a tratariva.

// Toda requisição interna é assíncrona,    '
