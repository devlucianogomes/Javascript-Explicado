// Trabalhando com dimensões
/**
 * Existem três grupos distintos de propriedades que podemos pegar no que tange a dimensões no javascript
 *
 * offsetWidth -> se trata da largura do elemento, porém ambos também pegam os valores de padding dados manualmente pelo usuario
 * offsetHeight -> se trata da altura do elemento
 */

// selecionando o elemento
let texto = document.querySelector(".texto");
console.log(texto);
