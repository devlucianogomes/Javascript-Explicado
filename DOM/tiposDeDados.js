// Veremos agora os tipos de dados presentes no Javascript

let nome = "Usuario"; //  String, ou seja, texto
let idade = 90; //  Number, ou seja, numero
let peso = 75.4; //  Float, ou seja, numeros com decimais
let possuiCnh = true; // Boolean, ou seja, verdadeiro ou falso
let cidade; // Undefined , ou seja, não foi definida

// Para checarmos o tipo de dado presente em uma varíável usamos o TYPEOF
console.log(typeof possuiCnh); // -> nos retornará Boolean
console.log(typeof nome); // -> nos retornará String
